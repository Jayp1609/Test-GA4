pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                // Use 'npm' command from the specified Node.js installation
                sh 'npm install'
            }
        }
    }
}
